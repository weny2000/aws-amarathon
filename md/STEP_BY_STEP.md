# 📖 逐步部署指南

## 🎯 解决 "Get Pages site failed" 错误

### 第一步：确认仓库状态

1. **检查仓库可见性**
   - 仓库必须是 **Public**（公开）
   - 如果显示 🔒 Private，点击 Settings → General → Change visibility

2. **确认文件已上传**
   - 检查仓库根目录是否包含所有必要文件
   - 特别确认 `.github/workflows/` 目录存在

### 第二步：正确配置 GitHub Pages

1. **进入 Pages 设置**
   ```
   仓库首页 → Settings 标签 → 左侧菜单 Pages
   ```

2. **配置 Source**
   ```
   Source 部分：
   ❌ Deploy from a branch (不要选这个)
   ✅ GitHub Actions (选择这个)
   ```

3. **保存设置**
   ```
   点击 Save 按钮
   ```

### 第三步：配置 Actions 权限

1. **进入 Actions 设置**
   ```
   Settings → Actions → General
   ```

2. **设置工作流权限**
   ```
   Workflow permissions:
   ✅ Read and write permissions
   ✅ Allow GitHub Actions to create and approve pull requests
   ```

3. **保存权限设置**
   ```
   点击 Save 按钮
   ```

### 第四步：手动触发部署

1. **进入 Actions 页面**
   ```
   仓库首页 → Actions 标签
   ```

2. **选择工作流**
   ```
   左侧选择 "Deploy static content to Pages"
   或 "Deploy to GitHub Pages"
   ```

3. **手动运行**
   ```
   点击 "Run workflow" 按钮
   选择 main 分支
   点击绿色的 "Run workflow" 按钮
   ```

### 第五步：等待部署完成

1. **监控部署状态**
   - 在 Actions 页面查看运行状态
   - 等待显示绿色 ✅ 成功标记

2. **获取网站地址**
   - 部署成功后，在 Settings → Pages 可以看到网站地址
   - 格式：`https://USERNAME.github.io/REPOSITORY`

## 🔧 如果仍然失败

### 方案 A：重置 Pages 配置

1. Settings → Pages → Source → **None** → Save
2. 等待 30 秒
3. Settings → Pages → Source → **GitHub Actions** → Save
4. 重新触发部署

### 方案 B：检查分支名称

1. 确认你的默认分支名称（main 或 master）
2. 如果是 master，需要修改工作流文件：
   ```yaml
   on:
     push:
       branches: ["master"]  # 改为 master
   ```

### 方案 C：使用简化工作流

如果复杂的工作流有问题，我们已经创建了 `.github/workflows/static.yml`，这是一个更简单的版本。

### 方案 D：强制重新部署

```bash
# 创建空提交触发部署
git commit --allow-empty -m "Force redeploy"
git push origin main
```

## ✅ 成功标志

当一切正常时，你会看到：

1. **Actions 页面**：绿色 ✅ 成功标记
2. **Settings → Pages**：显示绿色部署状态和网站链接
3. **网站可访问**：`https://USERNAME.github.io/REPOSITORY`

## 📞 需要更多帮助？

- 查看详细故障排除：`TROUBLESHOOTING.md`
- 检查 Actions 页面的详细错误日志
- 确保按照每个步骤的确切顺序操作