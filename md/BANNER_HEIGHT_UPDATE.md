# 横幅高度更新说明

## 更新内容

参照原始Amarathon网站，将横幅图片从自动高度改为固定高度（使用视口单位）。

## 高度设置

### 桌面端（> 1199px）
```css
.banner-section-full .banner-image {
    height: 15.625vw;  /* 约300px在1920px宽度下 */
}
```

### 平板端（768px - 1199px）
```css
.banner-section-full .banner-image {
    height: 48.828125vw;  /* 约500px在1024px宽度下 */
}
```

### 移动端（< 768px）
```css
.banner-section-full .banner-image {
    height: 74.6666666667vw;  /* 约280px在375px宽度下 */
}
```

## 为什么使用vw单位

### 优势
1. **响应式**: 高度随屏幕宽度自动调整
2. **比例一致**: 保持横幅的宽高比
3. **视觉统一**: 不同设备上保持相似的视觉效果
4. **与原网站一致**: 完全参照原网站的实现方式

### vw单位说明
- `1vw` = 视口宽度的1%
- `15.625vw` = 视口宽度的15.625%
- 在1920px宽度下：15.625vw ≈ 300px
- 在1024px宽度下：48.828125vw ≈ 500px
- 在375px宽度下：74.6666666667vw ≈ 280px

## 技术实现

### CSS代码

#### 桌面端
```css
.banner-section-full .banner-image {
    width: 100%;
    height: 15.625vw;
    display: block;
    object-fit: cover;
}

.banner-section-full .banner-placeholder {
    width: 100%;
    height: 15.625vw;
    min-height: 200px;  /* 最小高度，防止过小 */
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    font-size: 48px;
    font-weight: 700;
}
```

#### 平板端（768px - 1199px）
```css
@media screen and (max-width: 1199px) {
    .banner-section-full .banner-image {
        height: 48.828125vw;
    }
    
    .banner-section-full .banner-placeholder {
        height: 48.828125vw;
        min-height: 250px;
    }
}
```

#### 移动端（< 768px）
```css
@media screen and (max-width: 767px) {
    .banner-section-full .banner-image {
        height: 74.6666666667vw;
    }
    
    .banner-section-full .banner-placeholder {
        height: 74.6666666667vw;
        min-height: 200px;
    }
}
```

## 对比

### 之前（自动高度）
```css
.banner-section-full .banner-image {
    height: auto;  /* 根据图片原始比例 */
}
```

**问题**:
- 不同图片高度不一致
- 无法控制视觉效果
- 可能过高或过低

### 现在（固定高度）
```css
.banner-section-full .banner-image {
    height: 15.625vw;  /* 固定比例 */
    object-fit: cover;  /* 裁剪适应 */
}
```

**优势**:
- ✅ 高度一致
- ✅ 视觉效果可控
- ✅ 响应式适配
- ✅ 与原网站一致

## object-fit: cover

使用 `object-fit: cover` 确保图片填充整个容器：

```css
.banner-section-full .banner-image {
    object-fit: cover;
}
```

### 效果
- 图片会被裁剪以填充容器
- 保持图片的宽高比
- 不会变形或拉伸
- 居中显示

## min-height 保护

添加 `min-height` 防止在极小屏幕上高度过小：

```css
.banner-section-full .banner-placeholder {
    height: 15.625vw;
    min-height: 200px;  /* 最小200px */
}
```

### 作用
- 在超小屏幕上保持最小高度
- 确保内容可读性
- 防止布局崩溃

## 实际高度示例

### 桌面端（15.625vw）
| 屏幕宽度 | 横幅高度 |
|---------|---------|
| 1920px  | 300px   |
| 1680px  | 262.5px |
| 1440px  | 225px   |
| 1280px  | 200px   |

### 平板端（48.828125vw）
| 屏幕宽度 | 横幅高度 |
|---------|---------|
| 1024px  | 500px   |
| 768px   | 375px   |

### 移动端（74.6666666667vw）
| 屏幕宽度 | 横幅高度 |
|---------|---------|
| 414px   | 309px   |
| 375px   | 280px   |
| 320px   | 239px   |

## 浏览器兼容性

### vw单位支持
- ✅ Chrome/Edge (所有版本)
- ✅ Firefox (所有版本)
- ✅ Safari (所有版本)
- ✅ IE9+ (部分支持)
- ✅ 移动浏览器 (所有版本)

### object-fit支持
- ✅ Chrome/Edge 31+
- ✅ Firefox 36+
- ✅ Safari 7.1+
- ❌ IE11 (不支持)

### 降级方案
对于不支持 `object-fit` 的浏览器（如IE11），图片会被拉伸以填充容器，但不会影响基本功能。

## 优势

### 1. 视觉一致性
- ✅ 所有页面横幅高度一致
- ✅ 不同图片显示效果统一
- ✅ 专业的视觉呈现

### 2. 响应式设计
- ✅ 自动适应不同屏幕
- ✅ 保持合适的宽高比
- ✅ 移动端优化

### 3. 性能优化
- ✅ 固定高度避免布局抖动
- ✅ 提升页面加载体验
- ✅ 减少重排重绘

### 4. 与原网站一致
- ✅ 完全参照原网站实现
- ✅ 相同的视觉效果
- ✅ 一致的用户体验

## 注意事项

1. **图片尺寸**: 建议使用宽度至少1920px的图片
2. **图片比例**: 建议使用16:9或类似比例的图片
3. **重要内容**: 确保图片重要内容在中心区域，避免被裁剪
4. **测试**: 在不同屏幕尺寸下测试显示效果

## 图片准备建议

### 推荐尺寸
- **桌面端**: 1920x300px (16:1)
- **平板端**: 1024x500px (约2:1)
- **移动端**: 750x560px (约4:3)

### 安全区域
为避免重要内容被裁剪，建议：
- 重要文字和Logo放在中心区域
- 避免在边缘放置关键信息
- 使用居中构图

## 总结

横幅高度已更新为固定高度：
- 📏 使用vw单位实现响应式
- 🎨 与原网站风格一致
- 📱 完整的移动端支持
- ✅ 视觉效果统一
- 🚀 性能优化

所有修改已完成并通过验证！
